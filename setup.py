from distutils.core import setup

setup(
    name='SQLAlchemyOrderingList',
    version='0.1.0',
    author='Brian S Morgan',
    author_email='brian.s.morgan@gmail.com',
    packages=['sqlalchemy_orderinglist'],
    url='https://github.com/bmorgan21/sqlalchemy-orderinglist',
    description='Allow for adding constants on ordered relationships.',
    install_requires=[
    ],
    dependency_links=[
    ]
)
